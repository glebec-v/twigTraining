{% extends '_inner.twig'%}

{% block title%}
    {{ config.title }} Products
{% endblock %}

{% block content%}
<div class="container">
    <div class="divPanel page-content">
        <div class="row-fluid">
            <h1>Get your ship</h1>
            {% for item in storage %}
                {% include 'element.twig' %}
            {% endfor %}
        </div>
    </div>
</div>
{% endblock %}

